# A play web app

### Howto

`play idea` or `sbt gen-idea update-classifiers` (downloads sources)

(`sbt gen-idea` requires https://github.com/mpeltonen/sbt-idea)

### Start

`play idea`

### Build for cloudfoundry
`play clean compile`
`play -Dconfig.file=conf/cloud.conf dist`

### Deploy to cloudfoundry
`cf push`

### Troubleshooting

#### Idea won't compile

Mark `target/scala-2.10/classes_managed` and `target/scala-2.10/src_managed/main` as Source Folders

